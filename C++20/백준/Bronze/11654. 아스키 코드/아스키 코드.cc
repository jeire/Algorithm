#include <iostream>
#include <string>

using namespace std;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int a;
    char ss;
    cin >> ss;
    cout << (int)ss;
    return 0;
}